import React from 'react'

import styles from './AnimatedGrid.module.scss'

type Props = {}

const AnimatedGrid = (props: Props) => {
    return (
        <div className="animated-grid fixed inset-0 z-0"></div>
    )
}

export default AnimatedGrid